{
  "name": "webcrawler-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Workspace commands for the Webcrawler project",
  "scripts": {
    "test": "npm --prefix backend test",
    "test:backend": "npm --prefix backend test",
    "lint:frontend": "npm --prefix frontend run lint",
    "build:frontend": "npm --prefix frontend run build",
    "verify": "npm run test:backend && npm run lint:frontend && npm run build:frontend"
  },
  "engines": {
    "node": ">=18"
  }
}
